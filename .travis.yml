# LightTwinSVM Program - Simple and Fast
# Developer: Mir, A. (mir-am@hotmail.com)
# License: GNU General Public License v3.0

# Configuration file for building and testing LightTwinSVM on Travis CI
language: python

# Ubuntu 16.04 LTS & OS X
matrix:
  include:
    - os: linux
      dist: xenial
      sudo: true
      python: '3.5'
      
    - os: linux
      dist: xenial
      sudo: true
      python: '3.6'
      
    - os: linux
      dist: xenial
      sudo: true
      python: '3.7'
      
  allow_failures:
    - os: osx
      language: generic
      env: PYENV_VERSION=3.6.7

branches:
  only:
  - master
  - dev

addons:
  apt:
    packages:
    - liblapack-dev
    - libblas-dev
    - python3-dev
    - python3-tk

before_install:
  - chmod +x install-travisci.sh
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then echo $'#!/bin/bash\npython3 -m unittest discover -s tests' > ltsvm_test.sh; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then echo $'#!/bin/bash\npython -m unittest discover -s tests' > ltsvm_test.sh; fi
  - chmod +x ltsvm_test.sh
  
install: ./install-travisci.sh

script:
- ./ltsvm_test.sh
